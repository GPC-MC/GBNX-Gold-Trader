meta {
  name: Get Silver OHLC
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/pricing/ohlc/silver?interval=3600&limit=50&offset=0&sort=desc
  body: none
  auth: none
}

params:query {
  interval: 3600
  limit: 50
  offset: 0
  sort: desc
}

docs {
  Get OHLC (Open, High, Low, Close) data for Silver (XAG/USD)
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
